#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../music/bin/music" "$@"
  ret=$?
else 
  node  "$basedir/../music/bin/music" "$@"
  ret=$?
fi
exit $ret
